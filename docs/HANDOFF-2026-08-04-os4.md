# Handoff — Inference-X Phase A, next unit: OS-4

- Written: 2026-08-04
- Repo: `/home/coeusyk/projects/inference-x`, branch `develop`
- Next session focus: **OS-4 — Truthful resolution and typed degradation**

This document is orientation only. Everything substantive already lives in
repository artifacts; they are authoritative and this file is not.

## Read these first (in authority order)

1. `docs/DECISIONS.md` — DEC-047 (Engine Boundary), DEC-048 (lint/type baseline),
   DEC-049 (streaming contract), DEC-050 (superseded metrics), DEC-051 (seed)
2. `docs/PHASE-A-EXECUTION-PLAN.md` — §4 (OS-4 definition), §5 (touch points),
   §7.3 (merge/DEC-number reservation), §9 (acceptance criteria)
3. `docs/ARCHITECTURE.md`
4. `CONTRIBUTING.md`, `AGENTS.md`

For the shape a Phase A OpenSpec should take, read the two already archived:
`openspec/changes/archive/2026-08-04-add-truthful-token-accounting/` and
`.../2026-08-04-add-deterministic-generation/`. The first one's `proposal.md`
carries the **Compatibility Invariants** / **Intentional Behavioral Changes** /
**Resolved Ambiguities** sections the user asked for and reviews against — reuse
that structure for OS-4.

## Where Phase A stands

| Unit | State |
|---|---|
| OS-1 CI + static analysis gate | Merged (`dcd0736`). Required check `checks` enforced on `develop` and `main`. OpenSpec still **active at 25/26** — only task 5.2 (archive) is open. |
| OS-2 truthful token accounting | Merged (`344d4af`), OpenSpec archived. |
| OS-3 deterministic generation (`seed`) | Merged (`ed7c07a`), OpenSpec archived. |
| **OS-4 truthful resolution** | **Not started. This is the next unit.** |
| OS-5 benchmark suite integrity | Not started. Parallel-safe with OS-4. |
| OS-6 advisor honesty | Not started. Depends on OS-2 (done), so unblocked. |
| HK-1 housekeeping | Not started. No dependencies. |

Gate is green: **447 passed, 1 xfailed**, `ruff` clean, `mypy` clean.
The xfail is intentional — seed determinism under concurrency, deferred to Phase C3
(DEC-051). Do not try to make it pass.

Working tree is clean except one untracked file — see Open Items.

**Next free ADR number is DEC-052.** DEC-051 is the highest in
`docs/DECISIONS.md`. Claim the number at PR time, not in the proposal.

## Open items carried into the next session

1. **`docs/PHASE-A-EXECUTION-PLAN.md` is untracked.** The document every unit
   treats as authoritative is not in git. Worth committing before OS-4 references
   it again.
2. **OS-1's OpenSpec is unarchived** (task 5.2). Enforcement is confirmed; it can
   ride with HK-1 or a one-line archive PR.
3. Both OS-2 final-review blockers are **closed** — the `finish_reason="error"`
   yield was removed, and DEC-050 gained the clause explaining that a results
   directory cannot be partitioned pre/post-OS-2. Nothing outstanding there.

## OS-4 — what it is

Scope is defined in `docs/PHASE-A-EXECUTION-PLAN.md` §4 under "OS-4". Do not
restate or renegotiate it; write the OpenSpec against it.

Concern in one line: **the client can see every substitution the server made.**

Verified current state of OS-4's targets, so the OpenSpec can be written without
re-deriving it:

- `src/inference_x/routing/admission.py:91` — `getattr(engine, "count_prompt_tokens", None)`
  with a silent chars/4 fallback. OS-4 switches this to a declared method.
- `src/inference_x/routing/admission.py:212` — `getattr(engine, "kv_capacity_tokens", None)`.
  **This one stays on `getattr`.** DEC-047 §3 marks it *provisional* and forbids
  freezing it as a cross-backend contract; Phase B4 may delete it.
- `src/inference_x/engines/base.py` — `BaseEngine` currently declares only
  `generate`, `generate_stream`, `is_healthy`. OS-4 adds `count_prompt_tokens`.
- `src/inference_x/schemas/chat.py` — `ChatCompletionResponse` has
  `id/object/created/model/choices/usage` and no `warnings` or `resolved`.
  `ChatCompletionRequest` already carries `stream_options` (OS-2) and `seed`
  (OS-3); `strict` is OS-4's to add, appended last.

Request-schema ownership was pinned in OS-2's proposal (R4) and is binding:
`stream_options`→OS-2, `seed`→OS-3, `strict`+`warnings`+`resolved`→OS-4.
The effective-seed echo belongs in OS-4's `resolved` block, not OS-3.

## Traps that cost time this session

- **Verify stated assumptions against the repository.** Twice a prompt asserted
  something ("the OpenSpec is committed", "DEC-049 is accepted") that was false in
  the tree. Repository state wins. Check before implementing.
- **DEC-047 §4 forbids tightening admission to fail-closed.** OS-4 makes existing
  degradation *observable*; it must not change what admission decides.
- **Do not grow the DEC-048 mypy baseline** (4 modules: `vllm_engine`, `driver`,
  `chat_service`, `api/deps`). Growth is a review finding. `chat_service` could not
  be removed after OS-2 — its 3 residual errors come from `BaseEngine.generate_stream`
  being `async def` rather than `def` returning an AsyncGenerator. Fixing that is a
  separate contract change.
- **Do not touch `api/deps.py` or `engines/registry.py`.** The registry factory is
  deliberately deferred to the Phase A/B seam (plan §2.4) because Phase B re-signs
  the constructor.
- **`article-final.md` is gitignored under a deliberate policy comment.** It cannot
  be part of a repository diff and no acceptance criterion may depend on it.
- **OS-4 and OS-2 share the SSE generator in `services/chat_service.py`.** OS-2 has
  landed, so rebase cleanly; the conflict surface is known.
- **`playground/` is a client and stays unmodified.** It still displays
  `len(content.split())` counts in two places — a recorded follow-up, not OS-4 work.

## Working agreements the user enforces

- **Use context-mode and `rtk proxy`, not bare `Bash`.** `ctx_batch_execute` /
  `ctx_execute_file` for gathering and analysis; `rtk proxy <cmd>` for host-state
  mutation (git, installs). This was corrected mid-session — take it seriously.
  Native `Read` only when the next step is an `Edit`.
- **No `Co-Authored-By` trailer** in commits (saved memory).
- Commit only when asked.
- Stop and report rather than expanding scope. If a required change belongs to
  another unit, record it; do not implement it.
- Deliverable style: the user asks for explicit checklists (invariants, behavioral
  changes, acceptance criteria) and reviews mechanically against them.

## Suggested skills

- **`openspec-workflow`** — required. OS-4 needs a proposal + spec delta + tasks
  under `openspec/changes/`, and `AGENTS.md` forbids non-trivial work without an
  active change. Validate with `openspec validate <id> --strict`.
- **`andrej-karpathy-guidelines`** (`andrej-karpathy-skills:karpathy-guidelines`) —
  the user invoked this for every prior unit. Surgical changes, stated assumptions.
- **`ponytail:ponytail`** — active by default in this environment; keeps OS-4 from
  growing abstractions DEC-047 forbids.
- **`superpowers:brainstorming`** — only if OS-4's scope needs shaping before the
  OpenSpec is written. The plan already defines it, so this may be skippable.
- **`superpowers:verification-before-completion`** — the user checks claims against
  command output. Run the gate before asserting anything is done.
- Skip `superpowers:test-driven-development` unless asked; prior units wrote tests
  alongside implementation against a pre-agreed contract test.

## Useful commands

```bash
uv run pytest tests/unit -q          # 447 passed, 1 xfailed expected
uv run ruff check .                  # must be clean
uv run mypy src/                     # must be clean
uv run pytest tests/unit/test_engine_driver.py -q --count=20   # DEC-043 race guard
openspec list
openspec validate <change-id> --strict
```

Note: `tests/unit/test_engine_driver.py` guards a race that was ~40% flaky before
DEC-043. Any change touching `engines/driver.py` must pass it under `--count=20`.
OS-4 should not touch the driver at all.
